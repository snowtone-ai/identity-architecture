# Identity Architecture 研究OS — プロジェクト運用規則

これは理論構築の研究プロジェクトである。ソフトウェア開発ではない。

## セッション開始時
1. `state/research-state.md` を読む（これだけ。他は必要時にオンデマンド読込）
2. 現在のフェーズと進行中タスクを確認してから作業する

## 絶対規則（研究の完全性）
- 仮説を事実として書かない。すべての理論的言明は `theory/claims/` の主張ファイルとして登録し、ステータスを付与する
- 主張を「支持」に昇格させる前に、必ず反証探索を実施し記録する（workflows/W03 参照）
- `evidence/refutations/` のファイルは絶対に削除・改変しない（追記のみ可）
- 証拠には必ず等級（E1〜E5、governance/quality-standards.md 参照）を付ける
- 出典のない経験的主張を概念ファイル・主張ファイルに書かない
- 概念・主張・証拠の新規作成時は対応する `_TEMPLATE.md` を使い、INDEX.md に1行追記する

## 作業規則
- 1セッション=1タスクを原則とする。タスク完了時に `state/research-state.md` を更新してコミット
- 大量の文献読解・探索は librarian サブエージェントに委譲する（メインコンテキストを汚さない）
- 主張の批判的検証は critic サブエージェントで行う（新鮮なコンテキストで実施）
- レポート・対話は日本語。ファイル内容も日本語。ファイル名・IDはASCII

## 禁止事項
- theory/ 配下の一括リライト（変更は1ファイルずつ、理由をコミットメッセージに）
- 反証記録の削除
- INDEX.md の更新忘れ（ファイル新規作成とINDEX更新は同一コミット）

---

## 自動実行ルール（Claude Code 運用規則）

### 原則：人間のターミナル操作をゼロにする

Claude Code はすべてのファイル操作・git操作・状態更新を自律的に実行する。
人間に「このコマンドを実行してください」と指示することを禁止する。
実行できない理由がある場合のみ、理由を明示して例外を申告する。

---

### Git 自動コミット規則

以下のいずれかが発生したタイミングで、直ちに `git add -A && git commit` を実行する。
人間の確認を待たない。

| トリガーイベント | コミットメッセージ形式 |
|---|---|
| 主張ファイル（CL-XXX）の新規作成 | `CL-XXX: <主張の一文> を着想として登録` |
| 主張のステータス変更 | `CL-XXX: <主張の一文> を<新ステータス>に更新` |
| 証拠ファイル（E-XXX）の追加 | `E-XXX: <証拠タイトル> を CL-XXX に紐付け` |
| 反証記録（R-XXX）の追加 | `R-XXX: <反証概要> を記録` |
| 文献ノート（L-XXX）の作成 | `L-XXX: <著者Year> 文献ノートを作成` |
| 概念ファイル（C-XXX）の新規作成 | `C-XXX: <概念名> を概念台帳に登録` |
| 意思決定記録（DR-XXXX）の作成 | `DR-XXXX: <意思決定タイトル>` |
| research-state.md の更新 | `state: セッション状態を更新` |
| 週次レビュー（/weekly）の完了 | `weekly: <YYYY-MM-DD> 週次レビュー完了` |

コミット後、即座に `git push origin main` を実行する。

---

### オートコンパクト対策（コンテキスト50%ルール）

オートコンパクトはコンテキスト使用率 **50%** で発動する。
以下を各セッションの運用原則とする。

**セッション開始時:**
1. `state/research-state.md` を読み込む（他ファイルはオンデマンド）
2. 未プッシュコミットがあれば `git push origin main` を実行する

**作業中（常時）:**
- ファイルを読み込む前に、本当に必要かを判断する（不要な読み込みを避けコンテキストを節約）
- サブエージェント（librarian / critic / auditor）には独立したコンテキストを与え、
  メインコンテキストへの読み込みを最小化する

**コンパクト発動前に必ず実行する処理（各タスク完了時に実施）:**

進行中の作業内容・次のアクションを research-state.md に書き込む
git add -A && git commit -m "state: <作業中断前の状態を保存>"
git push origin main


**コンパクト後の再開時:**
- `state/research-state.md` の「次のアクション」セクションから即座に再開する
- 人間への確認なしに継続する

---

### スラッシュコマンドの完全自動実行

各コマンドは人間の追加入力なしに、対応するワークフロー（W01〜W06）を
最後の git push まで自律実行する。

- `/lit <テーマ>` → W01 を実行 → L-XXX 作成 → コミット＆プッシュ
- `/claim <着想>` → W03 を実行 → CL-XXX 作成 → コミット＆プッシュ
- `/critique <ID>` → W04 を実行 → R-XXX 作成（必要なら） → コミット＆プッシュ
- `/weekly` → W05 を実行 → research-state.md 更新 → コミット＆プッシュ

完了後は実施内容のサマリーのみを人間に報告する。

---

### 禁止事項

- 「以下のコマンドを実行してください」と人間に指示すること
- git 操作を後回しにすること（ファイル変更と git commit は不可分）
- research-state.md を更新せずにセッションを終了すること
- コンテキスト節約のため必要な処理を省略すること（省略する場合は明示する）
